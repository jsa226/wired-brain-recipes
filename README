# wired-brain-recipes
This README is for the Pluralsight couse Getting Started with Git 2.
These commands were used during the course:

config --list
init
branch -m main
config --global init.defaultBranch main
add .
commit -m "<comment>"
remote add origin https://github.com/<username>/<repo-name>.git
push -u origin main
status
add <filename>
status --short
diff --staged
diff --staged -no-renames
diff
commit -a -m "<comment>"
push origin main
log
log -1
log --oneline
log --stat
log --patch
rm --cached <filename>
checkout -b <branch-name>
checkout main
stash
stash list
stash show
merge <branch-name>
reset --mixed
clone https://github.com/<username>/<repo-name>.git
